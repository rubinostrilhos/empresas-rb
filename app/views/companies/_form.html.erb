<%= simple_form_for(@company) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>
  <div class="form-inputs">
    <%= f.input :email %>
  </div>

  <div class="form-actions btn btn-success">
    <%= f.button :submit %>
  </div>
<% end %>
